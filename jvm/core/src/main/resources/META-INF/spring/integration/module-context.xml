<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:int="http://www.springframework.org/schema/integration"
       xmlns:int-amqp="http://www.springframework.org/schema/integration/amqp"
       xmlns:rabbit="http://www.springframework.org/schema/rabbit"
       xmlns:int-stream="http://www.springframework.org/schema/integration/stream"
       xsi:schemaLocation="http://www.springframework.org/schema/integration/amqp http://www.springframework.org/schema/integration/amqp/spring-integration-amqp.xsd
		http://www.springframework.org/schema/integration http://www.springframework.org/schema/integration/spring-integration.xsd
		http://www.springframework.org/schema/integration/stream http://www.springframework.org/schema/integration/stream/spring-integration-stream.xsd
		http://www.springframework.org/schema/rabbit http://www.springframework.org/schema/rabbit/spring-rabbit.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

    <!-- Channels & gateways. No worries about underlying infrastructure -->
    <int:gateway service-interface="org.eigengo.sogx.core.RecogService" />
    <int:channel id="recogRequest"/>
    <int:channel id="recogResponse">
        <int:interceptors>
            <int:wire-tap channel="loggingChannel" />
        </int:interceptors>
    </int:channel>

    <!-- Logging & debugging -->
    <int-stream:stdout-channel-adapter id="consoleOut" channel="recogResponse" append-newline="true" />
    <int:logging-channel-adapter id="loggingChannel" log-full-message="true" level="INFO" />

    <!-- Route the messages on the ``recogRequest`` to the ``sogx.exchange`` using routing key ``si.recog.key`` -->
    <int-amqp:outbound-channel-adapter
            channel="recogRequest" amqp-template="amqpTemplate" exchange-name="sogx.exchange" routing-key="sogx.recog.key" />
    <!-- Route responses from the ``sogx.recog.queue`` to the channel ``recogResponse`` -->
    <int-amqp:inbound-channel-adapter
            channel="recogResponse" queue-names="sogx.recog.queue" connection-factory="connectionFactory" />

    <!-- RabbitMQ connection details -->

    <!-- note that default is the machine's hostname, which can cause problems, when we mean ``localhost`` -->
    <rabbit:connection-factory id="connectionFactory" host="localhost"/>
    <rabbit:template id="amqpTemplate" connection-factory="connectionFactory" />
    <rabbit:admin connection-factory="connectionFactory" />
    <rabbit:queue name="sogx.recog.queue" />

    <rabbit:direct-exchange name="sogx.exchange">
        <rabbit:bindings>
            <rabbit:binding queue="sogx.recog.queue" key="sogx.recog.key" />
        </rabbit:bindings>
    </rabbit:direct-exchange>

</beans>