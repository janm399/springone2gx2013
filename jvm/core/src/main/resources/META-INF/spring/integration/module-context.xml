<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:int="http://www.springframework.org/schema/integration"
       xmlns:int-amqp="http://www.springframework.org/schema/integration/amqp"
       xmlns:rabbit="http://www.springframework.org/schema/rabbit"
       xsi:schemaLocation="http://www.springframework.org/schema/integration/amqp http://www.springframework.org/schema/integration/amqp/spring-integration-amqp.xsd
		http://www.springframework.org/schema/integration http://www.springframework.org/schema/integration/spring-integration.xsd
		http://www.springframework.org/schema/rabbit http://www.springframework.org/schema/rabbit/spring-rabbit.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

    <!-- for the CLI application -->
    <bean id="executor" class="org.springframework.core.task.SyncTaskExecutor"/>

    <!-- Channels & gateways. No worries about underlying infrastructure -->
    <int:gateway service-interface="org.eigengo.sogx.core.RecogService" async-executor="executor"/>
    <int:channel id="recogRequest"/>
    <int:channel id="recogResponse"/>
    <int:channel id="rawRecogResponse"/>
    <int:channel id="rawBytesRecogResponse"/>

    <int:transformer input-channel="rawBytesRecogResponse" output-channel="rawRecogResponse" expression="new String(payload)"/>
    <int:json-to-object-transformer input-channel="rawRecogResponse" output-channel="recogResponse"
                                    type="org.eigengo.sogx.core.CoinResponse" auto-startup="true"/>

    <int-amqp:outbound-gateway request-channel="recogRequest" reply-channel="rawBytesRecogResponse"
                               exchange-name="sogx.exchange" routing-key="sogx.recog.key"
                               reply-timeout="250"
                               mapped-reply-headers="*" mapped-request-headers="*"
                               amqp-template="amqpTemplate"/>

    <!-- RabbitMQ connection details -->

    <!-- note that default is the machine's hostname, which can cause problems, when we mean ``localhost`` -->
    <rabbit:connection-factory id="connectionFactory" host="localhost"/>
    <rabbit:template id="amqpTemplate" connection-factory="connectionFactory"/>
    <rabbit:admin connection-factory="connectionFactory"/>
    <rabbit:queue name="sogx.recog.queue"/>

    <rabbit:direct-exchange name="sogx.exchange">
        <rabbit:bindings>
            <rabbit:binding queue="sogx.recog.queue" key="sogx.recog.key"/>
        </rabbit:bindings>
    </rabbit:direct-exchange>

</beans>